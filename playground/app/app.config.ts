import { defineAppConfig } from '#imports'

export default defineAppConfig({
  ui: {
    colors: {
      neutral: 'neutral',
      gray: 'neutral'
    }
  },
  uiPro: {
    pageGrid: {
      base: 'my-10'
    }
  }
})
