export default defineAppConfig({
  ui: {
    colors: {
      primary: 'reefgold',
      gray: 'neutral',
      neutral: 'neutral'
    },
    button: {
      slots: {
        base: 'cursor-pointer'
      }
    }
  }
})
