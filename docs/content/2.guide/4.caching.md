---
title: Caching
description: Optimize performance with server-side caching
---

# Caching

WPNuxt caches GraphQL responses server-side using Nitro route rules.

## How It Works

1. First request fetches from WordPress (~200-500ms)
2. Response is cached on the server
3. Subsequent requests use cache (~1-5ms)
4. With SWR enabled, stale content serves instantly while refreshing in background

## Configuration

```ts [nuxt.config.ts]
wpNuxt: {
  cache: {
    enabled: true,   // Default: true
    maxAge: 300,     // Default: 300 (5 minutes)
    swr: true        // Default: true
  }
}
```

| Option | Default | Description |
|--------|---------|-------------|
| `enabled` | `true` | Enable/disable caching |
| `maxAge` | `300` | Cache duration in seconds |
| `swr` | `true` | Stale-while-revalidate |

## When to Disable

```ts
// Development - see changes immediately
wpNuxt: {
  cache: {
    enabled: false
  }
}

// Real-time content
wpNuxt: {
  cache: {
    maxAge: 60,
    swr: false
  }
}
```

## High-Traffic Sites

```ts
wpNuxt: {
  cache: {
    maxAge: 3600,  // 1 hour
    swr: true
  }
}
```

## Cache Layers

| Layer | Scope | Duration |
|-------|-------|----------|
| Server (Nitro) | All users | Configurable |
| Client (nuxt-graphql-middleware) | Per browser | Session |

Both layers work together automatically.
